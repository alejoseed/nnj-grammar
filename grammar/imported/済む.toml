# Generated from Dictionary of Japanese Grammar
# Run: nnj-grammar --output table "　ちょっと顔を見せるだけで済むと思う。"
# Then fill in the [[patterns.steps]] below based on the table output.

[[patterns]]
id         = "済む"
name       = "済む"
jlpt       = ""  # fill in: N5 / N4 / N3 / N2 / N1
meaning_en = "Take/cost/need/etc. only ~; do/manage (without/only with); get by/get away (without/only with); be let off (without/only with); be enough (if); be all right (if); not to have to: there is no need for"
# example: 　ちょっと顔を見せるだけで済むと思う。

# TODO: add steps after running --output table on an example sentence
# [[patterns.steps]]
# surface = ""
# pos1    = ""
# pos2    = ""
# conj_form = ""